import NotificationList from "../components/NotificationList";

const Notifications = () => {
  return (
    <section className="
      w-full
      flex
      flex-col
      items-center
    ">
      <NotificationList />
    </section>
  );
};

export default Notifications;